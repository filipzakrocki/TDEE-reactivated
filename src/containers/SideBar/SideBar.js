import React from "react";
import "./SideBar.scss";

import Debug from "../../debug/debug";

const SideBar = () => {
  return (
    <section className={"SideBar"}>
      <Debug />
    </section>
  );
};

export default SideBar;
